how to export the pdf
1. launch frontend: npm run dev
2. make sure playwright is installed: `npx playwright install ` 
3. run the server pdf export: `node ./server/pdf-export.mjs http://localhost:5173 output2.pdf` 

